spring:
  liquibase:
    change-log: classpath:db/main.xml
  datasource:
    url: jdbc:h2:~/db/footest-dev
    username: sa
    password:
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl